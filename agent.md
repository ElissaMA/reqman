# 工程开发准则

**宗旨**：极简、复用、诚实、严谨、合规。杜绝臆断、过度设计、违规操作、文件混乱。


## 一、核心原则（最高优先级，不可违反）

| # | 原则 | 含义 |
|---|------|------|
| 1 | **极简** | 代码、逻辑、沟通，三者皆精简。禁止废话、过度设计与无效工作量。 |
| 2 | **复用** | 项目存量 → 开源 → 标准库。禁止重复造轮子。 |
| 3 | **严谨** | 新增修改必核对、自测、验证。全程有据。 |
| 4 | **诚实** | 存疑即查，不确定即确认。不臆断、不假装理解。 |
| 5 | **合规** | 遵循项目架构、目录规范、编码与流程标准。 |

---

## 二、决策阶梯（开发唯一执行顺序）

自上而下校验，通过方可开发：

1. **YAGNI** — 非刚需直接舍弃
2. **项目存量** — 已有模块/函数/逻辑优先
3. **开源方案** — GitHub / npm 成熟实现
4. **语言标准库** — 官方原生能力
5. **平台原生** — 系统/浏览器特性，减少第三方
6. **现有依赖** — 已装包优先，严控新增
7. **最小实现** — 最少代码、最简逻辑实现稳定功能

---

## 三、AI 协作守则

| ✅ 应做 | ❌ 忌做 |
|---------|--------|
| 查阅核实 | 臆断猜参 |
| 事前确认 | 模糊执行 |
| 人审落地 | 自判业务 |
| 复用存量 | 新增冗余 |
| 主动自测 | 跳过验证 |
| 恪守架构 | 随意破坏 |
| 坦诚存疑 | 假装通晓 |
| 审慎重构 | 盲目乱改 |
| **精简沟通** | 客套铺垫 |

### 场景对照

| 场景 | ✅ 正确 | ❌ 错误 |
|------|--------|--------|
| 调用函数 | 先读签名+docstring，确认参数 | 凭函数名猜参数 |
| 修改逻辑 | 向人确认方案：改什么、为什么 | 看代码"好像有问题"直接改 |
| 判断业务 | 出方案给人审 | 替人做业务决策 |
| 调用 API | 项目封装/标准库 | 新依赖解一行问题 |
| 代码测试 | 改完跑 pytest | "应该没问题"不验证 |
| 改架构 | 查蓝图+DI，遵循现有模式 | 另起新架构 |
| 不确定 | "这部分不确定，请提供 X" | 编造答案 |
| 重构 | 小步改，验证再继续 | 一次改 10 文件 |
| **沟通** | 直达要点，不铺垫 | 客套修饰、冗余解释 |

---

## 四、工作区规范

### 1. 目录结构
- 源代码：`src/`
- 测试：`tests/`
- 配置：`pyproject.toml`、`.gitignore`
- 文档：`README.md`、`CHANGELOG.md`

### 2. 文件命名
- 临时文件：禁止在项目根目录创建
- 正式文件：遵循项目现有命名规范

### 3. 违规处置
非法写入、垃圾留存、违规生成 → 立即终止并报错。脏提交/脏目录零容忍。

### 4. 变更约束规则
- 任何变更代码原有结构、样式、架构的计划，须经项目负责人（人工）同意后方可实施
- 总工负责审查并上报，最终决策权归项目负责人
- 此规则适用于但不限于：UI风格、模板结构、CSS方案、数据库模型、API接口

---

## 五、执行

1. 所有任务先跑决策阶梯，禁止直接编码
2. AI 全程恪守守则，杜绝臆断、盲改、跳过校验
3. 代码变更必自测；独立验证由测试工程师执行
4. 严格遵守工作区规范，全程整洁
5. 极简、复用、诚实、严谨。拒绝过度开发与无效工作
6. 沟通精简：直达要点，去客套去修饰。碎片化 > 长句，表格 > 散文

7. 分支工作流：
   - 数据更新走 `main`；UI/小改走 `dev`；大改走 `feature`
   - 开始 dev/feature 工作前：先 `git merge main` 同步最新数据
   - 提交前先 `git branch` 确认当前分支

8. 团队协作流程（所有任务统一执行）：
   - ① 总工发布任务（含验收标准、文件清单、禁止事项）
   - ② 工程师执行并自测
   - ③ 测试工程师独立验证
   - ④ 总工抽查审核
   - ⑤ 总工汇总汇报

9. 提交/合并/推送（三项分开，各自需用户确认）：
   - **提交**：汇报结果，等待用户确认后执行 `git commit`
   - **合并**：等待用户确认后执行分支合并
   - **推送**：等待用户确认后执行 `git push`
   - 严禁未经用户确认的 commit / merge / push，违反即违规

10. 任务模板：
    任务：#编号 <名称>
    - 目标 / 修改文件 / 验收标准 / 禁止事项
    - 不提交，等待总工确认
11. 汇报精简规范（控制上下文占用）：
    - 成员汇报：仅回传关键信息，删除冗长表格/逐条核对
      格式：任务：#编号 结果：✅/❌ | 改动文件：<列表> | 验证：<1行> | 待确认：是/否
    - 总工汇报：不复述成员细节，只做摘要 + 待确认项
      格式：✅ 任务完成（成员已验证）| <1行结果> | 待确认：是否提交/合并/推送
13. 测试流程：
    - 日常小改：仅运行相关 pytest（如 `pytest tests/test_card_service.py`）+ ruff
    - 提交时全量测试（必做）：
      1. `pytest -m "not slow"`（单元+集成）
      2. `pytest -m e2e`（有头浏览器，验证页面/弹窗/toast/变量渲染）
      3. `ruff check src/ tests/`
    - UI/交互改动必须附 e2e 验证证据
    - 测试数据用后即清，DB 保持与 git 一致
14. 版本与 CHANGELOG：
    - 每次提交必须更新 CHANGELOG.md（新增条目）
    - 版本号随提交递增：修复/小改 → 次版本+1（如 3.2.3）；功能/大改 → 次版本或主版本升级（3.3.0 / 4.0.0）
    - pyproject.toml version 与 CHANGELOG 同步
15. 沟通降噪：
    - 忽略多余 idle 通知，仅在新任务或需确认时响应
    - 成员不发送重复状态，一次汇报为准